version: '3.8'

services:
  espacio-backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    image: espacio-backend:latest
    container_name: espacio-backend
    volumes:
      - espacio-db-data:/app/data
    restart: always

  espacio-pwa:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
        - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}
    image: espacio-pwa:latest
    container_name: espacio-pwa
    ports:
      - "8080:80"
    depends_on:
      - espacio-backend
    restart: always

volumes:
  espacio-db-data:
